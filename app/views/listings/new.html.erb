<div id="center">
	<h1>Sell your copy of "<%= @book.title %>" </h1>
	<% form_for([@book,@listing]) do |f|%>
		<%= f.label :comment %>
		<%= f.text_field :comment %>
		<%= f.label :price %>
		<%= f.text_field :price%>
		<%= f.submit "submit"%>
	<% end %>	
</div><!--end center-->
